---
"webpack-dev-server": minor
---

Treat the `universal` platform (`compiler.platform.universal`, added in webpack `5.108.0`) as a web target so the client runtime is injected for `"universal"` and `["web", "node"]` targets.
